name: Build SyncWave

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Configure CMake
      run: cmake -B build -G "Visual Studio 17 2022" -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded

    - name: Build
      run: cmake --build build --config Release

    - name: Copy executable to repo root
      run: copy build\Release\SyncWave.exe SyncWave.exe

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: SyncWave
        path: SyncWave.exe

    - name: Commit updated executable
      if: github.event_name == 'push' && github.ref == 'refs/heads/master'
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add SyncWave.exe
        git diff --cached --quiet || git commit -m "Update SyncWave.exe [skip ci]"
        git push
